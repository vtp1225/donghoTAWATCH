/**
 * Định dạng số tiền tệ (hiện tại chưa có logic, trả về nguyên bản)
 * @param {number|string} value - Số tiền cần định dạng
 * @returns {number|string} Số tiền đã định dạng
 */
export function formatCurrency(value) {
  return value;
}
